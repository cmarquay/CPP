#include <iostream>
using namespace std;

int  main() {
  int x(0);
  double y(0);
  double a(0.0);
  double b(0.0);
  double c(0.0);
  double d(0.0);
  
  x=2;
  y=4;
  a=x+y;
  b=x-y;
  c=x*y;
  d=x/y;
  
  cout << "a vaut : "   << a << endl;
  cout << "b vaut : "   << b << endl;
  cout << "c vaut : "   << c << endl;
  cout << "d vaut : "   << d << endl;
  
  return 0;
}
